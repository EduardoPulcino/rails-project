<%= form_with(model: decoration) do |form| %>
  <% if decoration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(decoration.errors.count, "error") %> prohibited this decoration from being saved:</h2>

      <ul>
        <% decoration.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :event_type_id %>
    <%= form.collection_select :event_type_id, EventType.all, :id, :name, prompt: 'Selecione um tipo de evento' %>
  </div>

  <div class="field">
    <%= form.label :photo %>
    <%= form.file_field :photo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
